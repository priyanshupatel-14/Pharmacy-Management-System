{
  "buildCommand": "flutter build web --release --dart-define=API_URL=$API_URL",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
