# Development profile
# Obsolete H2 auto-initialization removed as the project strictly uses PostgreSQL and manual migrations.
# To run locally, provide PGHOST, PGPORT, PGDATABASE, DATABASE_USERNAME, DATABASE_PASSWORD in your environment.
